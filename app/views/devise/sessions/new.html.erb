<!DOCTYPE html>
<html lang="en">
<head>
  <title>Receptionist Robot Dog</title>
  <%= favicon_link_tag asset_path('favicon.ico') %>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<section class="section-dog">
 <body class="custom-background">
  <div class="row">
    <div class="col-1-of-3">
      <div class="card">
        <div class="card__side card__side--front">
          <body class="row justify-content-center mt-5">
                </br>
                <div class="text-center">
                  <%= image_tag 'DAISY.png', class: "mb-4", style: "width: 72px; height: 72px;" %>
                  <h1 class="h3 mb-3 font-weight-normal">Sign In</h1>
                  <div class="text-center">
                    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                      <div class="form-group">
                        <%= f.email_field :email, class: "form-control", placeholder: "Email", autofocus: true, autocomplete: "email" %>
                        <%= f.password_field :password, class: "form-control", placeholder: "Password", autocomplete: "current-password" %>
                      </div>
                      </br>
                      <div class="form-group">
                        <% if devise_mapping.rememberable? %>
                        <%= f.check_box :remember_me %>
                        <%= f.label :remember_me %>
                        <% end %>
                      </div>
                      </br>
                        <%= f.submit "Sign In", class: "btn btn-primary text-white" %>
                      <% end %>
                  </div>
                </div> 
            </body>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>







































