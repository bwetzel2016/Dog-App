<div class="card">
	<div class="card-header">
		New Appointment
	</div>
	<%= render 'form', post: @post %>

<div class="btn-group">
<%= link_to 'Back', posts_path, class: 'btn btn-secondary' %>
</div>
